import '/app/core/exporter.dart';

class TestGlobalWidget extends StatelessWidget {
  const TestGlobalWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
